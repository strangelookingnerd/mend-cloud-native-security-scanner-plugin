<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">

    <f:entry title="${%User Email}" field="userEmail">
        <f:textbox />
    </f:entry>
    <f:entry title="${%User Key}" field="userKey">
        <f:password />
    </f:entry>

    <f:entry title="${%Repositories}" field="repoNames">
        <f:textbox />
    </f:entry>

</j:jelly>
